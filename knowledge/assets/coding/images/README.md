将“回溯算法”的配图放在本目录下（`learning/coding/images/`），文件名需与 `learning/coding/materials.json` 中 `material_id=backtracking` 的 `images` 字段一致：

- `backtracking_2_template.png`

当前仓库内已存在对应的 PNG 文件，若你替换图片，请保持文件名一致：

- `backtracking_1_thinking.png`
- `backtracking_2_template.png`

放好后，进入课程「回溯算法」页面，在“🖼️ 图解”区域会自动展示这些图片。

